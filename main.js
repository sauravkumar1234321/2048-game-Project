const N = 4;
const MAX_VAL = 2048;
var randomValue = [];
var clock = new timer(new Date());
var count = 0;
var Score = new Score_class(0);
var grid = new Grid(N);
var show = 0;
var flag = 0;

gameManager(N);